<h1> <%= @category.name %> </h1>

<div class="container">
<ul><% @articles.each do |article| %>
    <li><%= link_to "#{article.title}    $#{article.price}", article_path(article) %></li>
    <% end  %>
</ul>
</div>

<%= link_to "Create new article", new_article_path(@category) %>
