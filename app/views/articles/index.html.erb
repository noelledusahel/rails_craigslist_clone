
<h1> <%= @category.name %> </h1>

<div class="container">
<ul><% @articles.each do |article| %>
    <li><%= link_to "#{article.title}    $#{article.price}", category_article_path(@category, article) %></li>
    <% end  %>
</ul>
</div>

<%= link_to "Create new article", new_category_article_path(@category) %>
